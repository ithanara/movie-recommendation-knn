# movie-recommendation-knn
Machine Learning project that builds a movie recommendation system using clustering (K-Means), dimensionality reduction (PCA), and similarity-based recommendations with K-Nearest Neighbors (KNN).
